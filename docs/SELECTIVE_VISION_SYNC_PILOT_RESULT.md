# 선택형 비전 LLM 동기 파일럿 정정 결과

## 결론

34쪽 동기 파일럿의 실행·비용 통제는 성공했지만, 목표연도까지 정확히 귀속된 구조화
필드 정확도는 `16/18(88.89%)`로 사전 기능명세의 확장 기준 `95%`를 통과하지
못했습니다. 추가 API 호출과 230쪽 확장은 중단합니다.

이전 보고서의 `73/258 로컬 자동 대조`를 품질 게이트로 사용한 판단과
`9/9 지표명·7/7 필드` 수치는 철회합니다. 로컬 파싱 실패 페이지를 비전 LLM에
보낸 실험에서 다시 로컬 파서 대조율을 핵심 기준으로 사용한 것은 순환 평가였고,
수기 파일의 PDF 물리 페이지와 문서 인쇄 페이지도 혼용했습니다.

## 실행 범위와 비용

- 모델: `gpt-5.6-luna`
- 요청: 34/34 성공, 반환 레코드 258개
- 성공 응답 토큰: 입력 91,602, 출력 31,506
- 성공 응답 비용: `$0.0561276`
- 절단 응답 재시도와 추적 누락 1건의 보수적 예약 포함: `$0.0750864`
- 승인 상한: `$0.10`
- 재채점 API 호출: 0회
- 입력 원본: 기존 34개 요청과 응답을 그대로 사용

동일한 요청 분포가 유지된다는 단순 비례값은 230쪽 약 `$0.508`, 후보 766쪽 전체
약 `$1.692`입니다. 현재 품질에서는 운영비 견적이나 도입 효과 산정값으로 사용하지
않습니다.

## 올바른 채점 기준

1. `source_pdf_page`는 PDF 파일의 1부터 시작하는 물리 페이지끼리 비교합니다.
2. `printed_page`는 실제 페이지 하단에 인쇄된 번호끼리 비교합니다.
3. `PDF p.494 / 문서 p.486`을 하나의 숫자로 축약하지 않습니다.
4. 수기 성과지표 424개 중 34쪽 입력과 물리 또는 인쇄 좌표가 연결되는 지표만
   페이지 표본의 정답으로 사용합니다.
5. 기대값이 실제 34쪽 입력 이미지에 보이는 필드만 필드 정확도 분모에 넣습니다.
6. 값이 응답 어딘가에 존재하는지와, 해당 값이 올바른 목표연도 레코드에 귀속됐는지를
   분리합니다.
7. 로컬 텍스트·OpenDataLoader 대조율은 보조 진단으로만 유지합니다.

## 정정 수치

| 지표 | 전체 | 계획서 | 보고서 |
|---|---:|---:|---:|
| 페이지가 연결된 수기 지표 | 19 | 16 | 3 |
| 지표명 회수 | 19/19 (100%) | 16/16 (100%) | 3/3 (100%) |
| 목표연도 레코드 연결 | 18/19 (94.74%) | 16/16 (100%) | 2/3 (66.67%) |
| 표본 입력에 실제 보이는 기대 필드 | 18 | 16 | 2 |
| 값 자체 회수 | 18/18 (100%) | 16/16 (100%) | 2/2 (100%) |
| 목표연도까지 올바르게 구조화 | 16/18 (88.89%) | 16/16 (100%) | 0/2 (0%) |

JSON 필수 키는 258/258 존재했습니다. 다만 스키마 키가 있다는 사실은 연도와 값의
귀속이 맞다는 뜻이 아닙니다.

## 보고서 3건 원문 확인

### 고용노동부 2022 보고서

- 수기 파일: PDF p.178 / 인쇄 p.174
- 재조정 근거: PDF p.66
- 실제 입력 PDF p.66 / 인쇄 p.62에 `근로지원인 수혜장애인 비율`, 실적 `1.89`,
  달성률 `113.9`가 모두 보입니다.
- LLM도 정확한 값이 든 레코드를 반환했지만, 같은 지표의 `신규 / -` 중복 레코드
  3개와 함께 반환했고 모든 레코드의 `fiscal_year`를 null로 냈습니다.
- 따라서 값 회수는 2/2이지만 목표연도 구조화는 0/2입니다.

### 고용노동부 2024 보고서

- 표본 입력 PDF p.165 / 인쇄 p.161에는 2024 목표 `69.0`까지만 있습니다.
- 실적 `69.5`와 달성률 `100.7`은 다음 PDF p.166 / 인쇄 p.162에 있습니다.
- p.166은 34쪽 표본에 포함되지 않았으므로 두 필드를 정확도 분모에서 제외했습니다.

### 보건복지부 2022 보고서

- 표본 입력 PDF p.246 / 인쇄 p.242에는 2022 목표 `36.5`까지만 있습니다.
- 실적 `36.6`과 달성률 `100.3`은 다음 PDF p.247 / 인쇄 p.243에 있습니다.
- p.247은 34쪽 표본에 포함되지 않았으므로 두 필드를 정확도 분모에서 제외했습니다.

## 해석과 다음 수정

- 계획서 표본은 16개 지표와 16개 목표값이 모두 올바른 연도에 구조화됐습니다.
- 보고서는 표가 페이지 경계에서 이어지고, 동일 지표의 연도 열을 여러 레코드로
  풀면서 연도 null·중복이 생기는 문제가 확인됐습니다.
- 목표연도 LLM 출력 114개 중 수기 지표명과 유사한 것은 53개(46.49%)였지만,
  수기 세트가 34쪽에 등장하는 모든 지표를 포함한다는 보장이 없으므로 이 비율은
  precision이나 정확도로 해석하지 않습니다.
- 다음 API 파일럿 전 로컬 수정 범위는 보고서에 한해 `현재 쪽+다음 쪽` 연결,
  동일 지표 중복 병합, 연도 열 귀속 강제입니다. 계획서 프롬프트와 호출 범위는
  당장 확대하지 않습니다.

## 2026-08-03 보고서 문맥 v2 로컬 준비

앞의 실패 원인 세 가지를 기존 하네스에서만 보강했습니다.

1. 보고서에서 실적 또는 달성률 레이블이 없고 다음 후보 페이지가 존재할 때만
   다음 페이지 이미지를 추가합니다. 전체 보고서 후보에 무조건 붙이지 않습니다.
2. 출력 JSON Schema의 `fiscal_year`를 요청 회계연도 단일 `enum`으로 제한해 연도
   null·다른 연도 레코드를 구조적으로 차단합니다. OpenAI Structured Outputs의
   지원 스키마에 포함된 integer·enum을 사용했습니다.
3. 같은 프로그램목표·지표명·회계연도 레코드는 비결측 값이 충돌하지 않을 때만
   병합합니다. 충돌하는 중복은 숨기지 않고 검수 대상으로 유지합니다.

기존 766쪽 OpenDataLoader 결과를 재사용해
`data/interim/selective_vision_pilot_report_context_v2/`에 요청 패키지를
재생성했습니다. 비전 요청 230건 중 직전 페이지 문맥은 15건, 다음 페이지 문맥은
8건입니다. 실패 사례인 고용노동부 2024 보고서 PDF 165→166쪽과 보건복지부 2022
보고서 PDF 246→247쪽이 모두 34건 파일럿 표본에 포함됐습니다.

34건 예상 동기비용은 `$0.063667`에서 `$0.066327`로 약 `$0.00266` 증가했고,
230건 전체 예상비용은 `$0.434897`에서 `$0.439597`로 약 `$0.00470`
증가했습니다. 실제 API 호출은 0회이며 `api_execution_allowed=false`를 유지했습니다.

다음 호출은 34건 전체 반복이 아니라 실패 원인을 대표하는 3건만 수행합니다.

- 고용노동부 2022 보고서 PDF 66쪽: 연도 null·동일지표 중복
- 고용노동부 2024 보고서 PDF 165~166쪽: 실적·달성률 다음 페이지
- 보건복지부 2022 보고서 PDF 246~247쪽: 실적·달성률 다음 페이지

3건 예상비용은 `$0.006431`, 출력 상한과 10% 안전여유를 포함한 최대비용은
`$0.009450`입니다. `api_execution_allowed=false`이면 실행 함수 자체가 중단되도록
추가 검증했습니다. 사용자 비용 승인 전에는 호출하지 않습니다. Ruff 전체 검사,
선택형 비전 단위테스트 15건, 전체 pytest 265건이 통과했습니다.

[OpenAI Structured Outputs 공식 문서](https://developers.openai.com/api/docs/guides/structured-outputs)

## 2026-08-03 실패 사례 3건 실제 회귀검증

사용자가 승인한 세 요청만 별도 패키지로 실행했습니다. 3/3 응답에 성공했고 입력
12,973토큰, 출력 1,867토큰, 실측 비용은 `$0.004835`였습니다. 승인 상한
`$0.009450`의 51.16%이며 호출 직후 `api_execution_allowed=false`로 다시
잠갔습니다.

| 사례 | 반환 결과 | 판정 |
|---|---|---|
| 고용부 2022 보고서 p.66 | 레코드 0개 | 연도를 확인하지 못한 값을 2022년으로 강제 생성하지 않아 오귀속·중복은 막았지만, 해당 지표 회수에는 실패 |
| 고용부 2024 보고서 p.165→166 | 2024년 7개 | 6개는 목표·실적·달성률을 연결했으나 마지막 `임금체불` 행은 지표명과 후속 값 연결 실패 |
| 복지부 2022 보고서 p.246→247 | 2022년 4개 | 4개 모두 목표·실적·달성률 연결 성공 |

11개 반환 레코드의 스키마와 회계연도는 모두 유효했고 다른 연도·null 연도 출력은
0건입니다. 같은 지표 중복 병합과 충돌은 실제 응답에서 발생하지 않아 각각 0건이며,
병합 산식 자체는 단위테스트로만 검증됐습니다.

수기 정답 12건 기준 지표명과 목표연도 레코드 연결은 각각 10/12(83.33%), 입력에
보이는 기대 필드의 목표연도 구조화는 20/23(86.96%)입니다. 반환된 11개 지표명은
모두 수기 지표와 연결됐고, 비교 가능한 수기 필드 일치율은 20/22(90.91%)였습니다.
세 건이 실패 사례에 치우친 회귀표본이므로 전체 정확도 추정치로 일반화하지 않습니다.

확장 기준 95%를 통과하지 못했습니다. 34건 재실행과 230건 확장, 자동승격은 계속
중단합니다. 다음 로컬 수정 후보는 페이지 경계에서 잘린 마지막 행만 대상으로
`현재 페이지 마지막 행 + 다음 페이지 첫 행`을 표 좌표로 연결하는 것입니다. 이를
검증한 뒤 같은 세 건을 다시 호출할지 별도 비용 승인을 받습니다.

## 2026-08-03 문맥 범위 v3 최종 회귀검증과 고도화 동결

v2 결과의 직접 원인은 다음 페이지 전체를 추출 대상으로 취급해, 문맥 페이지에서
새로 시작한 다른 지표까지 반환한 것이었습니다. v3에서는 세 가지만 최소 수정했습니다.

1. 현재 연도 머리글이 없는 페이지에는 직전 페이지를 자동 첨부합니다.
2. 직전·다음 페이지는 대상 페이지에서 시작된 행의 머리글·연도·후속 값 보완에만
   사용하고, 문맥 페이지에서 새로 시작한 지표는 반환하지 않도록 요청 범위를 고정합니다.
3. 정확도 평가의 표본 소속은 대상 물리 페이지로만 판단하고, 문맥 페이지는 원문
   근거 대조에만 사용합니다.

기존 OpenDataLoader raw 766건을 재사용해
`data/interim/selective_vision_pilot_report_context_v3/`을 생성했습니다. 후보 766쪽,
로컬 처리 536쪽, 비전 후보 230쪽은 그대로이며 직전 페이지 문맥은 15→25건,
다음 페이지 문맥은 8건입니다. 34건 예상 동기비용은 `$0.067221`, 230건 전체는
`$0.443960`입니다. 이 값은 운영 승인액이 아니라 후속 고도화의 단순 비용 기준선입니다.

최종 API는 같은 실패 사례 세 건만 실행했습니다.

| 대상 페이지 | 반환 지표 | 목표 | 실적 | 달성률 |
|---|---|---:|---:|---:|
| 고용부 2022 p.66 | 근로지원인 수혜장애인 비율 | 1.66 | 1.89 | 113.9 |
| 고용부 2024 p.165→166 | 15~64세 고용률 | 69.0 | 69.5 | 100.7% |
| 복지부 2022 p.246→247 | 전체 빈곤층 대비 복지수혜 비율 | 36.5 | 36.6 | 100.3% |

- 요청 3/3 성공, 반환 3건, 다른 연도·null 연도·중복 0건
- 수기 지표 회수 3/3, 목표연도 연결 3/3, 비교 필드 6/6 정확
- 스키마 유효 3/3, 확장 게이트 세 항목 100%
- 입력 15,552토큰, 출력 1,274토큰, 실측 비용 `$0.0046392`
- 승인 상한 `$0.009450` 준수, 응답 저장 `store=false`, 호출 직후
  `api_execution_allowed=false` 복구

로컬 문자열 대조에서 2건은 프로그램목표 번호 `I-1`과 원문 로마숫자 `Ⅰ-1`의 문자형
차이 때문에 레코드 전체 대조가 실패했지만, 지표명과 목표·실적·달성률은 수기값과 모두
일치했습니다. 이 보조 대조율은 핵심 정확도 게이트로 사용하지 않습니다.

세 건은 실패 원인을 겨냥한 회귀표본이므로 230쪽 전체 무인 정확도로 일반화하지
않습니다. 현재 코드·요청 패키지·응답·비용·한계를 고도화 체크포인트로 동결하고,
34건 재검증·230쪽 확대·자동승격은 **폐기나 종료가 아니라 후속 고도화 범위**로
보류합니다. 대회 MVP는 여기서 파이프라인 작업을 멈추고 수기 검증 성과데이터를 이용한
재정 데이터분석으로 전환합니다.
